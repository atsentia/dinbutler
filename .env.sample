# DinButler Configuration

# Anthropic API key (required for obox workflows)
ANTHROPIC_API_KEY=your_api_key_here

# GitHub token (optional, for private repos in obox)
GITHUB_TOKEN=

# Docker socket override (usually auto-detected)
# DINBUTLER_SOCKET=/path/to/docker.sock
